document.addEventListener("DOMContentLoaded", function() {
    // Exemplo básico de função para inicializar o mapa (a implementação real dependerá da API que você usar)
    function initMap(id, lat, lng) {
        const map = document.getElementById(id);
        // Implementar a lógica para inicializar o mapa aqui
        // Por exemplo, se você estiver usando o Google Maps API:
        // new google.maps.Map(map, {
        //   center: { lat: lat, lng: lng },
        //   zoom: 15
        // });
    }

    // Inicialização dos mapas para cada posto
   // initMap('map-distrito', -23.550520, -46.633308); // Exemplos de coordenadas
   // initMap('map-delta', -23.550520, -46.633308);
   // initMap('map-quati', -23.550520, -46.633308);
});
